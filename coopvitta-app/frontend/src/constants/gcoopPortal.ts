/** Portal do cooperado (Gcoop) — distinto do app interno COOPVITTA. */
export const GCOOP_AREA_COOPERADO_URL =
  import.meta.env.VITE_GCOOP_AREA_COOPERADO_URL?.trim().replace(/\/$/, '') ||
  'https://coopvitta-area-cooperado.evoluti.net.br';
