# Credenciais iniciais do OpenObserve (só na 1ª subida do container).
ZO_ROOT_USER_EMAIL=admin@coopvitta.cloud
ZO_ROOT_USER_PASSWORD=altere-esta-senha-forte

# Organização padrão (single-node)
OPENOBSERVE_ORG=default

# Gerado por scripts/render-otel-config.sh — base64(email:password)
# OPENOBSERVE_AUTH_BASIC=
